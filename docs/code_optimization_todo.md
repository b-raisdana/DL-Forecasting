# Code Optimization TODO

Backlog of optimization findings from the [optimization-review](../.claude/skills/optimization-review/SKILL.md) skill. Review only — nothing here is fixed yet; each item becomes its own later change, gated by that skill's mutation-safety check (for hot-path findings) and the normal pre-commit/incremental-ratchet gate ([infrastructure.md § incremental ratchet](infrastructure.md#incremental-ratchet-mypyruffxenon-scope)) when actually implemented.

Entry format, one line each: `- **[priority] \`file:line\` \`function\`** — weakness: ... Optimization: ... Factor: ... Hot path: ... Coverage: ... Mutation-safety: pending|done (date).` Priority tiers and factor definitions live in the skill, not repeated here.

- [P0](#p0)
- [P1](#p1)
- [P2](#p2)
- [done](#done)

## P0

Hot path, order-of-magnitude win, coverage already exists or is cheap to add — do next. Also covers confirmed-dead/duplicate code (near-zero risk regardless of hotness — see the skill's priority carve-out).

- **[P0] `app/helper/data_preparation.py:15-44` (module imports)** — weakness: 10 separate single-symbol `from helper.schema_casting import (X as X,)` statements instead of one. Optimization: merge into one `from helper.schema_casting import (all_annotations as all_annotations, apply_as_type as apply_as_type, ...)` statement — the `as X` re-export aliases stay (required by this repo's `mypy --strict` implicit-reexport rule), only the statement count shrinks. Factor: duplication/simplification. Hot path: N/A, import-time only. Mutation-safety: N/A (purely mechanical, no behavior change).
- **[P0] `app/helper/data_preparation.py:317-319`** — weakness: redundant mid-file `import os` / `import pandas as pd` (`pandas as pd` already imported at line 9; `import os` was never hoisted to the top-level block when the feather-write helpers were added). Optimization: delete the duplicate `import pandas as pd`, move `import os` into the top import block. Factor: duplication/simplification. Mutation-safety: N/A (mechanical).
- **[P0] `app/helper/schema_casting.py:96-166`** — weakness: `cast_and_validate2`, `apply_as_type2`, `column_dtypes` (free function) are a full unused "v2" variant — zero external callers confirmed via repo-wide grep (only referenced by each other inside this file). Optimization: delete all three (~70 lines). Factor: duplication/dead code. Hot path: N/A. Mutation-safety: N/A (unreferenced — nothing exercises this code, so nothing to regress).
- **[P0] `app/ai_modelling/dataset_generator/training_datasets.py:141`** (`_shapes_ok_or_log`) — weakness: `len(np.array(sc_slices[level][training_x_columns]))` allocates and copies a full ndarray purely to read its row count. Optimization: `len(sc_slices[level][training_x_columns])` — a DataFrame's `len()` is O(1), no copy. Factor: memory footprint / redundant computation. Hot path: yes, 4x per generated sample (`batch_size × dataset_batches`, default up to 100 batches, plus rejected-sample retries). Coverage: none found under `app/tests/` for `_shapes_ok_or_log`/`train_data_of_mt_n_profit` yet. Mutation-safety: pending (behavior-preserving in theory, but no test currently pins the shape-mismatch branch — write a characterization test for `_shapes_ok_or_log`'s True/False output first).
- **[P0] `app/helper/data_preparation.py:842-845`** (`concat`) — weakness: two dead `if ...: pass` no-op blocks. Optimization: delete both. Factor: duplication/dead code. Mutation-safety: N/A (no-ops, provably no behavior change).

## P1

Hot path with high risk (tests/mutation-safety check needed first) or moderate win, or cold path with a big win.

- **[P1] `app/PanderaDFM/ExtendedDf.py:114-186` (`ExtendedDf.read_file`) duplicates `app/helper/data_preparation.py:111-197` (`read_file`)** — weakness: two parallel implementations of the same read-file-or-generate-and-persist flow (near-identical docstrings, evidence they started as a copy), but only the free-function version has the new in-process LRU memo added this round — the classmethod path gets no caching benefit and the two will keep drifting. Optimization: make `ExtendedDf.read_file` delegate to `helper.data_preparation.read_file` (passing `cls.cast_and_validate`/`cls.schema_data_frame_model` through as the `caster_model`) instead of reimplementing the flow, per [cache-or-generate](../.claude/skills/cache-or-generate/SKILL.md)'s "one cache per artifact type, not one per caller." Factor: duplication/caching. Hot path: yes — base read path for every PanderaDFM-backed artifact going through `ExtendedDf`. Coverage: none found under `app/tests/` for `ExtendedDf.read_file` specifically. Mutation-safety: pending — real behavior consolidation, not mechanical; write characterization tests for both current call paths before merging them.
- **[P1] `app/helper/data_preparation.py:766-836` (`nearest_match`)** — weakness: hand-rolled forward/backward-fill merge (builds a union index, `ffill`/`bfill` + `shift`); the function's own inline comment already flags it (`# Todo: replace with pd.merge_asof`). Optimization: replace with `pd.merge_asof(direction=...)`. Factor: library delegation (lib-first). Hot path: yes — called from `PeakValley.py` and `ClassicPivot.py`'s structure-detection pipeline (per-dataset-generation-run, not per-sample). Coverage: none found under `app/tests/` for `nearest_match`. Mutation-safety: pending — genuine behavior-equivalence risk around the custom shift/direction semantics (the docstring's own worked examples are the natural characterization-test fixture); self-flagged as a Todo but not yet attempted, so budget real effort, not a quick swap.
- **[P1] `app/PreProcessing/encoding/rolling_mean_std.py` / `app/ai_modelling/training_data/PreProcessing/encoding/rolling_mean_std.py`** — weakness: near-byte-identical forks; this diff independently applied the same `write_data_file()` edit to both, reconfirming the double-maintenance cost is real, not hypothetical. Already tracked — see [docs/todos/03-infrastructure.md, todo step 12](03-infrastructure.md) ("delete superseded forks... keep the `ai_modelling` copy"). Not a new task, cross-referenced here as fresh evidence.
- **[P1] `app/PanderaDFM/ExtendedDf.py:71-72` (`ExtendedDf.new`)** — weakness: `cls.schema_data_frame_model.to_schema().columns.keys()` is recomputed inside `for key in dictionary_of_data.keys():` on every iteration instead of once before the loop. Optimization: hoist `schema_columns = cls.schema_data_frame_model.to_schema().columns.keys()` above the loop. Factor: redundant computation. Hot path: moderate (record/row construction, not the multi-thousand-sample training path). Coverage: none found under `app/tests/` for `ExtendedDf.new`. Mutation-safety: pending (behavior-preserving hoist, but no existing test pins current output — cheap to add given `new()`'s small input space).

## P2

Cold path regardless of win size, or any path with a marginal (<2x) win.

- **[P2] `helper/data_preparation.py:336` `_read_raw_data_file`** — weakness: the feather/ZSTD read path (`pd.read_feather`) always materializes the whole table into memory, then `_slice_rows()` applies `n_rows`/`skip_rows` via `.iloc` after the fact; the legacy CSV-zip path instead passes `nrows`/`skiprows` straight to `pd.read_csv`, which only parses the requested rows. Optimization: if a caller starts routinely requesting partial reads on large files, slice at the Arrow level instead (`pyarrow.feather.read_table(path).slice(skip_rows, n_rows).to_pandas()`) to avoid materializing rows that get discarded. Factor: memory/resource footprint. Hot path: no — `read_file()`/`read_without_index()`/`read_by_date()` are currently always called with `n_rows=None, skip_rows=None` repo-wide (no caller passes either), so this path is presently unexercised; revisit if that changes. Coverage: `app/tests/unit/helper/test_data_preparation.py` (memo layer only, doesn't exercise this branch). Mutation-safety: pending.
- **[P2] `app/ai_modelling/dataset_generator/training_datasets.py:332,335`** — weakness: `np.array(df_or_series)` used to drop to ndarray where this repo's own [vectorized-pandas-numpy](../.claude/skills/vectorized-pandas-numpy/SKILL.md) skill states a preference for `.to_numpy()`. Optimization: `x_dfs_by_level[level][-1].to_numpy()` / `sample.y_df.iloc[0][[...]].to_numpy()`. Factor: library delegation / micro-perf. Hot path: yes but marginal win (<2x, small arrays). Mutation-safety: N/A (behavior-identical swap; `np.array(x)` and `x.to_numpy()` produce the same values/dtype here).
- **[P2] `app/helper/data_preparation.py`** — weakness: commented-out dead code — `single_timeframe`'s alternate-implementation blocks (~lines 466-471, 477-478) and the fully commented `zz_test_index_match_timeframe` function (~lines 558-563). Optimization: delete (git history keeps it if ever needed). Factor: duplication/dead code. Mutation-safety: N/A (comments have no runtime effect).
- **[P2] `app/helper/data_preparation.py:665-669` (`map_symbol`)** — weakness: `upper_symbol in map_dictionary.values()` is an O(n) linear scan of dict values per call. Optimization: precompute/cache a value→key reverse lookup if `map_dictionary` ever grows past a handful of entries. Factor: algorithmic complexity. Hot path: no — flagged for completeness only, current mapping tables are small enough that this is noise today.
- **[P2] `app/FigurePlotter/now_review_notebook.py:77-98` (`load_cached_multi_timeframe_ohlcv`)** — weakness: reads a whole cached multi-timeframe file before slicing a requested two-week chart period. Optimization: when this notebook helper becomes slow, prefer a daily-file concatenation path or Arrow-level slicing for feather caches. Factor: memory/resource footprint. Hot path: no — interactive review notebook only. Coverage: smoke-checked through one BTC NOW sample and one BTC period chart. Mutation-safety: pending.
- **[P2] `app/infrastructure/disk_cache_layout.py:243-254` (`_schema_nullable_columns`) and `app/infrastructure/disk_cache_windowed.py:236` (`_read_cached_windows_via_duckdb`, `caster_model.validate(batched_df)`)** — investigate: whether a newer pandera release (or better use of the pandera API already in play) exposes a direct nullable-columns accessor in place of `_schema_nullable_columns`'s hand-rolled `try/except AttributeError` + `schema.columns.items()` scan. Factor: library delegation. Hot path: `_schema_nullable_columns` no (once per write, not per-row); `_read_cached_windows_via_duckdb`'s `validate()` call yes (per batched-window read). Mutation-safety: N/A until a concrete optimization is proposed.
- **[P2] `app/infrastructure/disk_cache*.py`** — investigate: survey the disk-cache read/write helpers for hand-rolled dtype/NaN/shape checks (e.g. `_disallowed_nan_columns` alongside `_schema_nullable_columns` above) that duplicate what a pandera `DataFrameModel` or pydantic runtime validation would already enforce via `caster_model.validate()`/`.to_schema()`, and could be deleted or replaced outright. Factor: library delegation. Hot path: mixed — depends which helper. Mutation-safety: N/A until a concrete optimization is proposed.

## done

Move a finished entry here with the commit it landed in (not delete it) — keeps a record of what was optimized, why, and how its mutation-safety check went.
